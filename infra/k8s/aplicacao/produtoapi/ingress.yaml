apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: produtoapi-ingress
  namespace: mcp-apis
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: "web"
spec:
  ingressClassName: traefik
  rules:
    - host: produtoapi.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: produtoapi
                port:
                  number: 80
