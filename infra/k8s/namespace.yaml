apiVersion: v1
kind: Namespace
metadata:
  name: mcp-apis
  labels:
    name: mcp-apis
    # Namespaces observed by the MCP must opt in to its NetworkPolicy egress.
    observability.mcp-apis.io/allow: "true"
