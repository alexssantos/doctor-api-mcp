apiVersion: v1
kind: Secret
metadata:
  name: grafana-admin-secret
  namespace: mcp-apis
type: Opaque
stringData:
  # NOTE: this is a placeholder strong credential for the local k3d lab cluster.
  # Rotate it (kubectl create secret ... --dry-run=client -o yaml | kubectl apply -f -)
  # and do NOT reuse this value anywhere else. For real environments, use a
  # proper secret manager (Sealed Secrets / External Secrets / Vault) instead
  # of committing credentials to git, even as a placeholder.
  GF_SECURITY_ADMIN_USER: admin
  GF_SECURITY_ADMIN_PASSWORD: "Chg3-Gf@Lab-9f2b7d1e"
